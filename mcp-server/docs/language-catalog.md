# Language catalog manifest

P5-06C exposes `language` and `languageScript` from `data/languages.json` as raw source-aware domains. Each uses a
stable `<domain>/<name>/<source>` ID; duplicate names require a source or ID. Fluff remains excluded.
