# Object catalog manifest

P5-06D exposes the named `object` collection from `data/objects.json` as raw source-aware records. IDs use
`object/<name>/<source>`; duplicate names require a source or stable ID. Fluff remains excluded.
