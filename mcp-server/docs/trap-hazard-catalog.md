# Trap and hazard catalog manifest

P5-06E exposes the separate named `trap` and `hazard` collections from `data/trapshazards.json` as raw,
source-aware records. IDs use `<domain>/<name>/<source>`; duplicate names require a source or stable ID. Fluff
remains excluded.
