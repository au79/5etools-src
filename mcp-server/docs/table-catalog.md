# Table catalog manifest

P5-06G exposes the named `table` collection from `data/tables.json` as raw source-aware records. IDs use
`table/<name>/<source>`. Table rows, labels, and styles retain their raw tabular structure; rendering is deferred to
E1. Fluff remains excluded.
