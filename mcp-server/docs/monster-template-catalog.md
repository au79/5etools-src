# Monster template catalog manifest

P5-07B exposes `monsterTemplate` and `legendaryGroupTemplate` from `data/bestiary/template.json` as separate raw,
source-aware domains. IDs use `<domain>/<name>/<source>`. Template application objects remain raw; no monster or
legendary-group mutation is performed.
